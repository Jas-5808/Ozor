# Решение проблемы с предупреждением безопасности браузера

## 🎯 Проблема
Браузер показывает предупреждение "Your connection to this site is not secure" для `https://ozar.uz` потому что используется самоподписанный SSL сертификат.

## ✅ Решения

### Вариант 1: Принять сертификат в браузере (Рекомендуется для разработки)

1. **Откройте** `https://ozar.uz` в браузере
2. **Нажмите** "Дополнительно" или "Advanced"
3. **Нажмите** "Перейти на ozar.uz (небезопасно)" или "Proceed to ozar.uz (unsafe)"
4. **Сайт откроется** и будет работать нормально

### Вариант 2: Добавить сертификат в доверенные (Для постоянного решения)

#### В Chrome/Edge:
1. Откройте `https://ozar.uz`
2. Нажмите на замок в адресной строке
3. Выберите "Сертификат" → "Подробности"
4. Нажмите "Копировать в файл" и сохраните сертификат
5. Откройте Настройки → Безопасность → Управление сертификатами
6. Импортируйте сохраненный сертификат в "Доверенные корневые центры сертификации"

#### В Firefox:
1. Откройте `https://ozar.uz`
2. Нажмите на замок → "Подключение не защищено" → "Подробнее"
3. Нажмите "Просмотр сертификата" → "Загрузить"
4. Откройте Настройки → Приватность и безопасность → Сертификаты → "Просмотр сертификатов"
5. Импортируйте сертификат в "Центры сертификации"

### Вариант 3: Получить настоящий SSL сертификат (Для продакшена)

Для получения настоящего SSL сертификата от Let's Encrypt:

1. **Настройте DNS** так, чтобы `ozar.uz` указывал на ваш сервер
2. **Откройте порты 80 и 443** в файрволе
3. **Запустите команду:**
   ```bash
   sudo certbot --nginx -d ozar.uz -d www.ozar.uz
   ```

## 🌐 Текущий статус

- ✅ **HTTPS работает** - `https://ozar.uz` доступен
- ✅ **HTTP → HTTPS перенаправление** работает
- ✅ **Сайт полностью функционален**
- ⚠️ **Предупреждение браузера** - нормально для самоподписанного сертификата

## 🚀 Результат

После принятия сертификата в браузере, `https://ozar.uz` будет работать без предупреждений!
